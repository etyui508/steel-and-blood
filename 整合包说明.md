# 硬核枪械生存

Minecraft 1.21.1 + NeoForge 21.1.236，137 个模组（截至 sbw_gore 1.25.1）。

## 这东西是什么

不是把一堆模组塞进一个文件夹。整包围绕**一条枪械生存线**来做：

1. **枪是稀缺品，不是开局就有的。** 开局只有基础物资，枪械要走蓝图 —— 火绳枪 → 手枪 → 步枪 → 狙击。
2. **伤是真的会拖垮你。** 中弹不是掉血条：流血、骨折、脑震荡会持续掉血、影响跑跳，最后影响你端枪的手。
   这套来自我们自己的 **SBW Gore**，并且和 **Legendary Survival Overhaul** 的肢体系统打通了。
3. **口渴和温度。** LSO 负责，喝水和保温是生存的一部分，季节（静谧四季）会改变难度。
4. **尸潮有节奏。** 夜晚和血月会成批来，逼你把据点修起来。
5. **任务和队伍。** FTB Quests 驱动进度，FTB Teams 让队友共享。

## 模组构成（按支柱）

| 支柱 | 模组 |
| --- | --- |
| 枪械 | 卓越前线 Superb Warfare、SBW Gore（自研）、燧发枪 Simple Musket |
| 生存 | Legendary Survival Overhaul（口渴/体温/肢体）、静谧四季、农夫乐事、苹果皮、尸体 Corpse、旅行者背包、Medical Remedies |
| 威胁 | Zombie Awareness、The Hordes、Undead Nights、Enhanced AI、Rotten Creatures、Fungal Infection: Spore、Friends&Foes、Enhanced Celestials |
| 持枪敌人 | SBW Gore 的**匪帮**（自研）、被配置成持枪的那部分普通怪物 |
| 世界 | Terralith、When Dungeons Arise、YUNG's Better Dungeons、Dungeons and Taverns、超多生物群系、自然罗盘 |
| 任务/队伍 | FTB Library、FTB Quests、FTB Teams、FTB Ultimine |
| 体验 | JEI、Jade、传送石碑、Xaero 小地图/世界地图、BetterF3、物品栏 HUD+、控制、鼠标手势、3D 皮肤层、First-person Model、越肩视角、Not Enough Animations、IMBlocker（输入法）、I18nUpdateMod（自动汉化） |
| 优化 | Lithium、Sodium + Sodium Extra、Iris、ModernFix、FerriteCore、EntityCulling、ImmediatelyFast、Chunky、alltheleaks、FastSuite、FastFurnace、zfastnoise、More Culling、Leaves Be Gone、Particle Core、AI Improvements、ScalableLux、Clumps、spark、fast-ip-ping |

## 任务线（5 章 39 个任务）

全部是我们自己写的，`config/ftbquests/quests/chapters/`：

| 章节 | 任务数 | 内容 |
| --- | --- | --- |
| 主线：枪与血 | 11 | 工作台 → 火绳枪 → 绷带夹板 → 手枪蓝图 → 格洛克 17 → 步枪蓝图 → AK-47 → 手雷 → 蝴蝶雷 → AWM，支线「活着回来」奖励创造弹药盒 |
| 生存：水与温度 | 7 | 温度计 → 水壶 → 净水器 → 御寒/避暑衣物 → 季节日历 → 自己种水 |
| 医疗：把人拖回来 | 7 | 囤绷带 → 囤夹板 → 急救箱 → 医疗包吗啡 → 强心针 → 心脏容器（永久加血上限） |
| 威胁：白天属于你，夜晚不一定 | 7 | 杀僵尸/骷髅/苦力怕 → 100 只尸潮 → 手雷 → 防弹插板 → 加特林 |
| 探索：找到它们 | 7 | 自然罗盘 → 传送石碑 → 地图 → 侦察 → 蓝图猎人 → 下界 → 世界观 |

### 改任务

进游戏按 `Ctrl` 打开任务书 → 右上角铅笔进编辑模式。**建议就用编辑器改**，比手写 SNBT 稳。

文案在 `config/ftbquests/quests/lang/zh_cn.snbt`（键名 `quest.<id>.title` / `.quest_desc` / `.quest_subtitle`）。

### ⚠️ 手写任务书必须注意：id 只能是"首位 0-7 的 16 位十六进制"

FTB Quests 把对象 id 当**有符号 long** 解析。如果 id 首位是 `8`-`F`（比如 `A100000000000001`），
它会溢出 → 解析失败 → **静默重新生成一个随机 id**。后果有两个，都很隐蔽：

1. 文案全丢 —— lang 里的 `quest.<旧id>.title` 指向的 id 已经不存在了
2. **依赖指向失效的 id 时，那个任务会被直接删掉**（我们的支线任务就这么没的）

所以手写章节时 id 用 `0`-`7` 开头。已经踩过一次，全部重写过了。

## 开局物资（需要你操作一次）

用了 Serilum 的 **Starter Kit**：它给新玩家发一套自定义背包。配置是"在游戏里摆好背包，然后 `/sk set 名字`"，
所以这一步得进游戏一次：

```
/sk set 开局
```

建议的开局物资（摆好后执行上面那条）：

- 绷带 ×3、夹板 ×2（`/sbwgore kit` 一键给齐）
- 面包 ×4、水壶类饮水 ×2（LSO 的）
- 石剑 ×1、火把 ×16
- **不要给枪** —— 枪走任务线

### 管理员命令

```
/sk list            看当前激活的包
/sk give <玩家> 开局   手动发
/sk reset           清空"已领过"记录，所有人重新领
/sk reload          改完配置重载
```

## 和 SBW Gore 的联动

SBW Gore 1.2.0 里带了 LSO 的数据文件：

- `data/sbw_gore/legendarysurvivaloverhaul/body_damage/damage_sources/*.json` ——
  按伤害类型把枪伤分配到肢体上：`gunfire` → 随机一条肢体，`gunfire_headshot` → 头，
  爆炸 → 全身，蝴蝶雷 → 腿脚。LSO 的 `LivingDamageEvent.Pre` 会读它，对玩家生效。
- `.../body_damage/consumables/bandage.json`、`splint.json` ——
  我们的绷带和夹板同时治疗 LSO 的肢体（数值在这两个文件里调）。

## 已知待办

- 启动器里第一次跑一次，确认能进主菜单、任务书能打开、没有 "Duplicate mod" 报错。
- 开局物资还没设（见上）。
- 尸潮强度（The Hordes / Undead Nights）和 LSO 的掉血速度都要按实际手感调。
- 服务器要用的话，客户端专属模组（Sodium/Iris/小地图/第一人称/IMBlocker 等）不必装到服务端。

## 版本约束（改版本前先看这里）

- **加载器是 NeoForge 21.1.236，不要随手升级模组。** 已经踩过一次：JEI 19.40 之后的每个版本都要求
  `neoforge [21.1.238,)`，装上直接 mod loading 崩溃。现在用的是 **19.39.0.372**，是能接受 21.1.236 的
  最新一版。
- 想让 JEI 用最新版，得先把 NeoForge 升到 21.1.238+，那是一次整体升级，别只换 JEI。
- 换任何模组之前先跑一遍同目录的三个检查脚本（重复 modId、缺失前置、加载器版本），
  它们抓出的问题都是"游戏直接起不来"那一类。

## 皮肤

- **万用皮肤补丁 CustomSkinLoader 14.27**（`ForgeV3` 版，这个 jar 自己声明了 `neoforge [20.2,)`，
  NeoForge 上能正常加载）。
- 配置在 `config/CustomSkinLoader/CustomSkinLoader.json`，皮肤源按顺序：
  **LittleSkin**（`https://littleskin.cn/csl/`，你的登录站）→ Mojang 兜底。
  只装 jar 不配这个文件是没用的，默认只会去 Mojang 拉。
- 配合包里的 **[3D 皮肤层] skinlayers3d**，皮肤的外层会渲染成立体的。
- 换皮肤站的话改 `loadlist` 第一项的 `root` 即可，路径要以 `/csl/` 结尾。

## 已移除的模组（别再加回来）

- **SBW Aimbot（`sbw_aim`）** —— 名字看着像"瞄准辅助"，实际上是自瞄 + ESP 透视（jar 里就是
  `Aimbot.class` / `EspRenderer.class`，mod 名直接叫 "SBW Aimbot"）。这是从 `丢老谋世界`
  复用模组时只看文件名搬进来的失误。硬核生存包里放这个，整条枪械发展线就没意义了。
  文件在 `/tmp/removed-mods/`，没删。

## 第一人称

包里有 **First-person Model**（第一人称能看到自己的身体和手脚）和 **越肩视角重制** 两个模组，
都在接管第一人称相机，默认配置会打架：

- `config/firstperson.json`：`enabledByDefault: true`、`vanillaHandsMode: OFF`（正确，不用动）
- `config/shouldersurfing-client.toml`：**`keep_camera_out_of_head_distance_multiplier` 已从 0.75 改成 0**
  —— 它原本的作用是"相机贴近头部时隐藏玩家模型"，而第一人称就是相机在头里，
  等于把 First-person Model 要画的身体直接抹掉。设为 0 关掉这条规则。

如果第一人称还是没有身体，按顺序排查：`F6`（切换键位）是否被按下 → 越肩视角是否停在越肩模式
（`[perspective] remember_last_perspective = true` 会记住上次的视角）→ 看 `logs/latest.log` 里
`FirstPersonModel` 的两行加载日志。

### 拿枪时自动切回原版手部

手上有枪的时候让 First-person Model 画玩家身体会很怪 —— 卓越前线自己有一套第一人称枪械模型
（开镜、后坐、换弹动画都在那套里），套在玩家模型手上会穿模。

已经在 `config/firstperson.json` 的 `autoVanillaHands` 里列了 **43 把卓越前线的枪**
（含 RPG / Javelin / 毒刺 / 电击枪 / 修理工具）和 3 项火绳枪，共 54 项。这个列表是
**独立判定**的：拿枪 → 切回原版手部（看到 SBW 的枪），空手或拿别的 → 看到自己的身体。

名单是用「`models/item/*.json` ∩ `sbw/guns/*.json`」求交集生成的，不是手抄的。
以后 SBW 加新枪，用同样的办法重新生成一遍：

```bash
# 交集就是"既是物品又有枪械定义"的那些，即真正的枪
unzip -l superbwarfare*.jar | grep -oE "models/item/[a-z0-9_]+\.json"
unzip -l superbwarfare*.jar | grep -oE "sbw/guns/[a-z0-9_]+\.json"
```

注意 `autoVanillaHands` 只认**精确物品 id**，不支持 `#标签`，所以必须逐个列。

---

## 持枪 NPC（sbw_gore 1.20.0 起）

硬核枪械包最大的破绽是：**只有你有枪**。这一轮补上了另一半 —— 会开枪的敌人。

### 匪帮

`sbw_gore:bandit`，中文名「匪帮」，卓越前线武器的人形使用者。

- **不是亡灵**：白天不烧，会开门，会捡地上的东西，不会因为离得远就消失。
- **每次都有枪**（第一天的自然生成除外，见下），而且枪里是真的有子弹：一个满弹匣 +
  3 个弹匣的备弹。它会像你一样换弹、打到枪身过热、打空之后拔刀冲上来。
- **按天数换装备**：
  | 天数 | 能roll到的武器 |
  | --- | --- |
  | 第 1~2 天 | 格洛克 17/18、M1911、MP443、MP5、马林、自制霰弹枪 |
  | 第 3~5 天 | AK-47、M4、HK416、QBZ95、SKS、Mk14、Vector、M870、AA-12、狩猎步枪、K98 |
  | 第 6 天起 | M60、RPK、SVD、Insidious、莫辛纳甘、Devotion、M98B、AWM、Sentinel、M2HB、Minigun |
  发射器（RPG/Javelin/毒刺/榴弹）**永远不会**给 NPC —— 隔着窗户拆家的敌人不叫敌人，叫拆迁队。
- **掉落**：枪 + 剩余备弹的一半（上限一个弹药类型上限）。前期没有弹药线的时候，
  打死一个匪帮就是最便宜的补给 —— 这也是为什么备弹是有限的，不是无限刷。
- **生成**：夜晚的黑暗处，和普通怪物共用刷怪上限，每组 1~2 只。第 1 天晚上生成的是**拿刀的**
  匪帮（`armedMobMinDay` 控制），免得开局就是一场对着手枪的石头剑决斗。

### 普通怪物持枪

同一套机制也能挂到原版怪物上。默认白名单：僵尸、尸壳、溺尸、僵尸村民、骷髅、流浪者、
掠夺者、卫道士 —— **第 2 天起**每只 5% 的概率带枪（Easy ×0.6，Hard ×1.4）。
实测 200 只僵尸里 6 只带枪。

想加别的模组的怪，把它的实体 id 加进 `armedMobs` 就行，不需要知道那个模组是什么。

### 配置（`config/sbw_gore-common.toml`，`[gunNPCs]` 段）

| 键 | 默认 | 说明 |
| --- | --- | --- |
| `gunNpcsEnabled` | true | 总开关。关掉之后匪帮还会刷，只是不带枪 |
| `armedVanillaMobs` | true | 普通怪物是否可能带枪 |
| `armedMobChance` | 0.05 | 每只怪带枪的概率（难度倍率另算） |
| `armedMobMinDay` | 2 | 第几天开始允许持枪（1 = 第一天就开） |
| `armedMobs` | 上面那 8 种 | 白名单，写实体 id |
| `tierWeaponsByDay` | true | 是否按天数分武器档次 |
| `reserveMagazines` | 3 | NPC 带几个弹匣的备弹 |
| `shootDistance` | 26 | NPC 想保持的交战距离（格） |

觉得太狠就把 `armedMobChance` 降到 0.02、`armedMobMinDay` 提到 3；
觉得太软就反过来。**先把难度调好再动别的。**

### 调试用

```
/give @s sbw_gore:bandit_spawn_egg      # 刷一只持枪匪帮
/summon sbw_gore:bandit                  # 同上，命令版
/kill @e[type=sbw_gore:bandit]           # 清场
```

刷怪蛋和 `/summon` 出来的一定是持枪的（只有自然生成才看天数）。

⚠️ `/summon` **不要带 NBT**：原版在带 NBT 的分支里不调用 `finalizeSpawn`，所以
`/summon sbw_gore:bandit ~ ~ ~ {Tags:["x"]}` 出来的是个空手的普通人。要打 tag 就
`/summon` 之后再 `/tag`。

---

## 卡壳与排除故障（sbw_gore 1.21.0 起）

枪会坏。每开一枪都有一点概率卡壳，卡住的枪**谁都打不响** —— 你也好，匪帮也好。

- **越容易卡的情况**：枪管过热（连续扫射最伤枪）、积碳（每发都在累积，只有换弹和排障会清）、
  全自动射击、自己身上有骨折或失血。
- **排除故障：按 N**。轻卡壳约 1 秒，重卡壳约 2.5 秒。这期间不能开火、不能换弹，
  把枪收起来会暂停进度。排完之后积碳减半，等于顺手擦了枪。
- **提示**：卡壳那一刻会有一声"干响"，准星下方常驻一行 `⚠ 卡壳 · 按 N 排除故障`；
  枪没坏的时候什么都不显示。
- **NPC 一样会卡**，会自己排（40 tick）。所以别指望对手永远是满弹匣。

### 键位（改过，注意）

`N` 原来是卓越前线的**切换射击模式**，现在归"排除枪械故障"。已经在这两处改好：

| 功能 | 原键 | 现键 |
| --- | --- | --- |
| 排除枪械故障（本模组） | —— | **N** |
| 切换射击模式（卓越前线） | N | **Y**（方向键上下也可以切） |
| 关闭语音聊天（简单语音） | N | `-` |

想换回来随便换：`设置 → 控制`，两个模组的键都能改。

### 配置（`config/sbw_gore-common.toml`，`[jams]` 段）

| 键 | 默认 | 说明 |
| --- | --- | --- |
| `jamsEnabled` | true | 总开关 |
| `jamChancePerShot` | 0.0035 | 干净、不热的枪每发概率（约每 5 个弹匣一次） |
| `severeJamChance` | 0.25 | 卡壳时有多大概率是重卡壳 |
| `heatFactor` / `carbonFactor` | 1.0 | 过热与积碳的影响倍率，设 0 就是关掉这一项 |
| `clearTicks` / `clearSevereTicks` | 22 / 52 | 轻重卡壳的排除耗时（tick） |
| `armedNpcsJam` | true | NPC 会不会卡壳 |

觉得烦就 `jamChancePerShot` 降到 0.001，或者 `jamsEnabled` 直接关掉。

### 调试

```
/sbwgore jam              # 让自己的枪卡住（也可以 /sbwgore jam @e[type=sbw_gore:bandit]）
/sbwgore jam severe       # 重卡壳
/sbwgore jam clear        # 立刻排掉（万一卡住了排不掉，用这个）
/sbwgore jam selftest     # 报告卡壳拦截是否仍然生效
```

---

## 回血只能靠药（sbw_gore 1.22.0 起）

**吃饱不再自动回血。** 原版的被动回血（饱食度/饱和回血）从源码层面关掉了，连它本来要扣的饱食度
也一起关 —— 所以不会出现"血条闪一下又掉回去"的画面。

**金苹果、再生药水、信标、任何模组的免费回血一律不算数**：血量无故上涨会在同一 tick 内被撤销，
客户端看不到闪动。用"药"的时候会开一个短暂窗口，允许回血。

**绷带现在真的回血**：止住两层流血 + 恢复 4 点生命（两心）。

**绷带的判定是"流血或受伤"**：没流血但有伤也照样能用（回血），两样都不占时它会明确告诉你
"没什么好包扎的"——不会默默没反应。**手上拿着绷带/夹板时，对断腿的人潜行右键 = 给他治疗**，
而不是把他扛起来（扛人是空手/拿别的东西时的手势）。

算作"药"的物品（可在 `[healing]` 段改）：

| 来源 | 物品 |
| --- | --- |
| 本模组 | 急救绷带 |
| 卓越生存 LSO | 绷带 / 石膏 / 医疗包 / 急救包 / 吗啡 / 治疗草药 / 补剂 |
| 药水 | **默认不算**（`allowPotions = false`）。打开它等于连金苹果的再生也放行，所以没开 |

配置（`config/sbw_gore-common.toml`，`[healing]` 段）：

| 键 | 默认 | 说明 |
| --- | --- | --- |
| `onlyMedicineHeals` | true | 关掉就恢复原版回血 |
| `medicineItems` | 上面那一串 | 算作药的物品 id |
| `medicineWindowTicks` | 80 | 用药后允许多久内回血（tick） |
| `allowPotions` | false | 药水/再生算不算药 |
| `bandageHealing` | 4.0 | 绷带回多少血（1.0 = 半心） |
| `healingWarning` | true | 被驳回的回血要不要在动作栏提示一句 |

调试：

```
/sbwgore healcheck        # 替身喂饱 + 打到 10 血 + 跑 200 tick 食物逻辑，看还回不回血
/sbwgore itemcheck        # 替身病人跑五种情况：绷带（完好/受伤/流血）、夹板（好腿/断腿）
/sbwgore selfcheck        # 自尽机制：正常能开火 / 转向自己时打不响 / 扣扳机真的会死
```

---

## 最后一个选择：把枪口转向自己（sbw_gore 1.23.0 起）

**按住 Z** —— 手里的枪翻过来对着自己，下方出现提示；**再点左键** —— 结束。
**松开 Z 就什么都没发生**，所以不会误触。

- 消耗一发子弹（空枪不行），死亡信息"X 给了自己一枪"，尸体和掉落照旧，没有奖励。
- 转向自己的时候枪**打不响前方**，不会误伤前面的队友。
- **默认任何时候都能用**。这是刻意的：只有系统认定你没救了才出现的机制，是电影桥段不是选择。
  想改成"临终专用"，把 `selfShot.onlyWhenThereIsNoWayOut` 打开即可
  （需要同时满足：血量很低 + 大出血/骨折/感染 里中两项）。
- 键位：`Z`（旅行者背包的"切换工具"已挪到 `[`）。

配置（`config/sbw_gore-common.toml`，`[selfShot]` 段）：

| 键 | 默认 | 说明 |
| --- | --- | --- |
| `enabled` | true | 总开关 |
| `onlyWhenThereIsNoWayOut` | false | 只在"没救了"的时候才允许 |
| `pastSavingHealth` / `pastSavingSystems` | 4.0 / 2 | "没救了"的判定标准 |
| `costsARound` | true | 是否消耗子弹（空枪不行） |
| `hints` | true | 屏幕提示 |

### 隐藏成就

这个包里有一个**隐藏成就**，达成方式不写在这里（写出来就不叫隐藏了）。
线索只有一条：它和上面这条机制有关，而达成的那一刻，提示会正好出现在那一帧。

进度界面里现在有一棵属于本模组的树叫「钢与血」—— 以后这个包的隐藏成就都会挂在那上面。

---

## 幸存者（sbw_gore 1.24.0 起）

这个包以前只有两种人：**你**和**匪帮**。现在有第三种 —— 那个自己活过第一个星期、拿着厨刀、
还没决定怎么看你的人。

- **中立**：他会自己打僵尸和尸潮，不主动打你；你先动手他会还手。
- **招募**：右键递给他 **3 颗绿宝石**（`recruitCost` 可调）。绿宝石是唯一没人能吃的货币。
- **招募之后他就跟你走**，而且你可以随便给他东西：
  - **护甲** → 穿身上
  - **武器**（枪/剑/斧）→ 拿手里
  - **其它（弹药、绷带）** → 进他的背包
  - 换下来的旧装备**还给你**，重新配装只要几次右键
- **他开枪要消耗你给他的弹药**：从背包里的弹药扣，打空了他就掏刀上去砍。
  和匪帮不一样 —— 匪帮有无限虚拟备弹，**你的兵要真的喂**。
- **初始装备很寒酸**：破衣服 + 可能一块皮甲 + 一把铁剑，运气好才有手枪；**绝不会自带步枪**，
  背包是空的。
- 白天出现，不占刷怪上限，不消失；死了背包里的东西掉一地。
- 刷怪蛋 `/give @s sbw_gore:survivor_spawn_egg`。

配置：`config/sbw_gore-common.toml` 的 `[survivors]` 段（总开关 / 招募价格 /
是否严格消耗背包弹药 / 背包格数）。

调试：
```
/sbwgore survivorcheck     # 招一个替身幸存者，验证弹药能从背包计数并被换弹扣掉
```

---

## 生病与吃药（sbw_gore 1.25.0 起）

除了伤口感染，现在还有三种**不需要开枪也会得**的病，以及一整套药（全部只用原版材料造）。

| 病 | 怎么得的 | 症状 | 怎么好 |
| --- | --- | --- | --- |
| **感冒** | 在冷的地方待太久（体温过低 / 雨天 / 冷水 / 高海拔冷夜） | 移速 -5%、打喷嚏 | **暖和的地方会自己好**（约 1 分钟），睡觉更快；感冒药立刻见效 |
| **流感** | 感冒拖久了恶化；**玩家之间会互相传染**（4 格内 35% 概率） | 移速 -15%、伤害 -20% | 只有**抗病毒药**；感冒药只能缓解一档 |
| **腹泻** | 生肉 / 腐肉 / 蜘蛛眼 / 没净化的水 | 攻击 -10%，持续扣饱食度和口渴 | 止泻药、抗生素、青霉素 |

| 药 | 配方 | 治什么 |
| --- | --- | --- |
| 青霉素 | 蘑菇 + 糖 + 玻璃瓶 | 伤口感染（**只降一档**） |
| 抗生素 | 青霉素 ×2 + 木炭 | 伤口感染（清干净） |
| 强效抗生素 | 抗生素 ×2 + 金粒 | 伤口感染（清干净 + 3 分钟不再发炎） |
| 感冒药 | 蜂蜜瓶 + 糖 + 蘑菇 | 感冒 |
| 抗病毒药 | 感冒药 ×2 + 荧石粉 | 流感（唯一） |
| 止泻药 | 木炭 + 糖 + 玻璃瓶 | 腹泻（唯一） |

**抗生素只管伤口感染** —— 感冒、流感、腹泻它都不管（药不对症只会提示一句，不浪费物品）。

**中期量产**：用机械动力的**加热混合器**：蘑菇 ×4 + 糖 ×2 + 玻璃瓶 ×2 → 青霉素 ×6。

**吃错药不治**（抗生素治不了流感，青霉素治不了感冒），但只提示一句、不惩罚也不浪费物品 ——
物品提示里会写明"可治愈：XX"。

配置在 `config/sbw_gore-common.toml` 的 `[diseases]` 段（总开关 / 流感是否传染 / 脏东西致病概率 /
危险食物清单）。调试：`/sbwgore diseasecheck`（六条药症对应 + 两条用错药）。

---

## 幸存者也会生病（1.25.1 起）

- 幸存者在雨里 / 冷地方待久了会**感冒**：会打喷嚏（听得见）、跑得慢。
- 手里拿对症的药 **右键他**＝给他治（和给他装备同一个手势）。
- 带着流感的幸存人在旁边咳，**你也会被传染**。
- 任务书新增第十章「别死在感冒上」，从青霉素一路教到给幸存者喂药。

---

## 键位总表（本包改动过的）

原则：**只挪必须挪的** —— 新机制、以及"多个模组抢同一个键"的情况才动，其余保持各模组默认。

**本包新增机制**

| 键 | 功能 |
| --- | --- |
| `Z` | 把枪口转向自己（按住 → 左键；松开可反悔） |
| `N` | 排除枪械故障 |
| `;` | 检视武器 |

**为冲突而调整**

| 动作 | 原键 | 现键 | 原因 |
| --- | --- | --- | --- |
| 切换射击模式（卓越前线） | `N` | `Y` | `N` 归排障（方向键上下同效） |
| 关闭语音聊天（简单语音） | `N` | `F7` | 让出 `N`，并避开小地图缩放 `-` |
| 切换背包工具（旅行者背包） | `Z` | `[` | `Z` 归自尽 |
| 麦克风静音（简单语音） | `M` | `Insert` | 原来按 M 会同时开地图 + 静音 + 切战术地图 |
| 战术地图（卓越前线） | `M` | `Home` | 与 FTB 地图重复 |
| 隐藏任务图标（FTB 任务） | `H` | `,` | `H` 归身体部位面板（本包常看） |
| TCT 核心按键 | `H` | `\` | 同上 |
| 死亡记录（尸体模组） | `U` | `End` | `U` 归越肩换肩（战斗常用） |

**有意保留的重叠**（不影响正常战斗，都是上下文相关的）：`V`（按住说话 / 近战 / 诱饵）、
方向键（越肩微调 + 切换弹种）、`左Ctrl`（冲刺 / 屏息 / 载具）、鼠标左右键（开火 / 开镜 + JEI 界面）、
`X`（枪械交互 / 载具）、`C`（自由视角 / 火炮）。

⚠️ **改 `options.txt` 之前必须先关游戏**：游戏运行时改这个文件，退出时会用内存里的键位覆盖回去。
核对或修正键位（游戏关着时跑）：

```
python3 tools/fix_keybinds.py "/mnt/d/666/新建文件夹/.minecraft/versions/钢与血(Steel & Blood)" --check
```

去掉 `--check` 就会把上表所有键位一键写好。
